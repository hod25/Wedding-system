#############################################
# Example environment configuration
# Copy this file to `.env` and fill in real values.
#############################################

# Strong random key for bot API authentication (generate with scripts/generate_bot_key.ps1)
BOT_API_KEY=CHANGE_ME

# Public base URL of the deployed site (Render)
REMOTE_BASE_URL=https://your-render-service.onrender.com
WEBSITE_URL=https://your-render-service.onrender.com

# Displayed inside invitation message
COUPLE_NAMES=הוד ונעם
WEDDING_DATE=2026-01-01

# Flask secret key (generate another random hex string)
SECRET_KEY=CHANGE_ME_SECRET

# Database URL (leave as-is for local sqlite, set to external DB if used)
# DATABASE_URL=sqlite:///instance/wedding.db

# Optional explicit python version hint
PYTHON_VERSION=3.13.4

# Any other custom overrides can go below
