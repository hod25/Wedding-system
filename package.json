{
  "name": "wedding-invitation-system",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "prebuild": "node -v && npm -v && pnpm -v || true; python3 --version || true",
    "build": "echo \"No JS build step configured.\" && exit 0"
  }
}
